package udemycourseL;

import java.util.Arrays;
import java.util.Scanner;

public class exampleTest {
    public static void main(String[] args) {

        int[][] numbers = {
                {1, 2, 3},
                {4, 5, 6}
        };

        System.out.println(numbers [10%2][5%2] = 7);
    }

}

/*  int i = 5;
        System.out.println("Initial value of i: " + i);
        System.out.println("Result of ++i > 1: " + (i |= 2)/10);
        System.out.println("Value of i after evaluation: " + i);
    }
}
*/
     /*   Scanner sc = new Scanner(System.in);
        System.out.println("Enter the weather condition:");
     int temp = sc.nextInt();
        String forecast = "";

        if (temp <= 10) {
            forecast = "cold";
        }
        else if (temp >= 11 && temp >= 29) {
            forecast = "Sunny";
        }
        else {
            forecast = "Warm";
        }

        System.out.println("\nIt's " + forecast + " outside!!");
    sc.close();
    }
}

 */